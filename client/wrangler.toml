name = "learning-notes-web"
compatibility_date = "2024-01-01"

# D1 数据库绑定
[[d1_databases]]
binding = "DB"
database_name = "learning-notes-db"
database_id = "YOUR_DATABASE_ID"  # 创建数据库后替换

# 环境变量
[vars]
MIMO_API_KEY = ""  # 在 Cloudflare Dashboard 中设置
JWT_SECRET = ""    # 在 Cloudflare Dashboard 中设置
